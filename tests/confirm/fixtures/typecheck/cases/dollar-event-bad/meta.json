{
  "id": "dollar-event-bad",
  "description": "$event on native @click must reject an unknown method",
  "expectErrors": true,
  "minErrors": 1,
  "mustMatch": ["TS2339", "notAMethod", "does not exist"],
  "requires": ["dom-events"],
  "expectMention": ["notAMethod"]
}
