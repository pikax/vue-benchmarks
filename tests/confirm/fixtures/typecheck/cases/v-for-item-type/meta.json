{
  "id": "v-for-item-type",
  "description": "v-for alias must carry the array element type into template expressions",
  "expectErrors": true,
  "minErrors": 1,
  "mustMatch": ["TS2339", "toUpperCase", "does not exist", "number"],
  "expectMention": ["toUpperCase"]
}
