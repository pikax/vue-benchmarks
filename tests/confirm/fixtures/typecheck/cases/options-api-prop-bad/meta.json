{
  "id": "options-api-prop-bad",
  "description": "Options API: assigning this.count (number) to a string must error",
  "expectErrors": true,
  "minErrors": 1,
  "mustMatch": ["TS2322", "string", "number"],
  "requires": ["script-types"],
  "expectMention": ["count"]
}
