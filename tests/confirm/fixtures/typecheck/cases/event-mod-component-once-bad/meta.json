{
  "id": "event-mod-component-once-bad",
  "description": ".once on a component emit does not erase the payload type",
  "expectErrors": true,
  "minErrors": 1,
  "mustMatch": ["TS2322", "TS2345", "number", "string", "not assignable"],
  "requires": ["emits"],
  "expectMention": ["string"]
}
