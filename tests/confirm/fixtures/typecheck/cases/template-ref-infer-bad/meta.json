{
  "id": "template-ref-infer-bad",
  "description": "useTemplateRef inferred from the template ref must reject a method missing on HTMLInputElement",
  "expectErrors": true,
  "minErrors": 1,
  "mustMatch": ["TS2339", "notAMethod", "does not exist"],
  "expectMention": ["notAMethod"]
}
